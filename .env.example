# Public HTTPS origin, without a trailing slash.
PUBLIC_BASE_URL=https://photos.example.com

# Private address reachable only from this host/container. Do not point this at
# a publicly exposed Immich instance.
IMMICH_URL=http://192.0.2.10:2283
